name: take_my_money
description: A new Flutter application.
publish_to: 'none'
version: 1.0.2+2

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.0
  in_app_purchase: ^0.3.4+12
  url_launcher: ^5.7.7
  rflutter_alert: ^1.1.0
  purchases_flutter: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true

  assets:
    - assets/